interface:
  display_name: "Paper Ingest Classifier"
  short_description: "Extract, classify, and archive PDFs"
  default_prompt: "Use $paper-ingest-classifier to ingest and classify a local research PDF."
